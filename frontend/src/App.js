// React app is replaced by the static dashboard (index.html + dashboard-v61.js)
// This component renders nothing — the dashboard JS handles all UI.
function App() {
  return null;
}

export default App;
